#include <bits/stdc++.h>

// Greedy

//Soulzioni che gaurdano il problema attuale, non quello dopo
//Es. Decisoni attuali come il turno;

//Dinamica
/*
std::vector<int> v;
std::vector<int> prefix;

int sum(int a, int b)
{
    return prefix.at(b) - prefix.at(a - 1);
}
*/
int main()
{
    /* std::vector<long long int> v(1000);
    v.at(0) = 1;
    v.at(1) = 1;
    v.at(2) = 2;
    for (int i = 3; i < 101; ++i)
    {
        v.at(i) = v.at(i-1) + v.at(i-2);
    }
    std::cout << v.at(100);
    */

    /*
    int N;
    std::cin >> N;
    v.resize(N + 1);
    for (int i = 1; i <= N; ++i)
    {
        std::cin >> v.at(i);
    }
    prefix.resize(N + 1);
    for (int i = 1; i <= N; ++i)
    {
        prefix.at(i) = prefix.at(i - 1);
    }
    */


}
